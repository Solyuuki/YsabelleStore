#!/usr/bin/env sh
set -eu

# This hook is intentionally read-only. Member and sprint metadata updates are explicit through
# `npm run status:update -- --member m1`; the full documented workflow is `prepush:local`.
npm run verify:code
