#!/usr/bin/env sh
set -eu

# Pushes are fail-closed for canonical database/image state. The guard refreshes the
# Sprint canonical ref, rejects stale bases and frozen-history rewrites, then the
# existing read-only code verification runs the migration/state integrity suite.
npm run state:push:guard
npm run verify:code
