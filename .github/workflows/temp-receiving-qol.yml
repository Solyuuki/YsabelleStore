name: Temporary Receiving QoL Refactor

on:
  push:
    branches:
      - sprint/v0.10/sprint-10
    paths:
      - .github/workflows/temp-receiving-qol.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: sprint/v0.10/sprint-10
          fetch-depth: 0
      - name: Apply Receiving pagination and return history
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def read(path):
              return Path(path).read_text()

          def write(path, text):
              Path(path).parent.mkdir(parents=True, exist_ok=True)
              Path(path).write_text(text)

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected exactly one match, found {count}")
              return text.replace(old, new, 1)

          # ---------------- backend validators ----------------
          path = "backend/src/validators/restock.validators.ts"
          text = read(path)
          text = replace_once(
              text,
              '''const optionalSearchSchema = z.preprocess((value) => {
  if (typeof value === "string") {
    const trimmed = value.trim();
    return trimmed.length > 0 ? trimmed : undefined;
  }
  return value;
}, z.string().max(160).optional());
''',
              '''const optionalSearchSchema = z.preprocess((value) => {
  if (typeof value === "string") {
    const trimmed = value.trim();
    return trimmed.length > 0 ? trimmed : undefined;
  }
  return value;
}, z.string().max(160).optional());

const optionalBooleanSchema = z.preprocess((value) => {
  if (value === "true") return true;
  if (value === "false") return false;
  return value;
}, z.boolean().optional());
''',
              "validators optional boolean"
          )
          text = replace_once(
              text,
              '''export const cancelRestockOrderSchema = z.object({
  expectedVersion: z.coerce.number().int().min(0),
  reason: z.string().trim().min(3).max(500)
});
''',
              '''export const cancelRestockOrderSchema = z.object({
  expectedVersion: z.coerce.number().int().min(0),
  reason: z.string().trim().min(3).max(500)
});

export const saveRestockReturnReportSchema = z.object({
  expectedVersion: z.coerce.number().int().min(0),
  supplierName: z.string().trim().min(2).max(160),
  deliveryReference: optionalTextSchema(120)
});
''',
              "validators return report schema"
          )
          text = replace_once(
              text,
              '''export const restockOrderListQuerySchema = z.object({
  search: optionalSearchSchema,
  status: restockOrderStatusSchema.optional(),
  statuses: optionalRestockStatusesSchema,
  page: z.coerce.number().int().min(1).default(1),
''',
              '''export const restockOrderListQuerySchema = z.object({
  search: optionalSearchSchema,
  status: restockOrderStatusSchema.optional(),
  statuses: optionalRestockStatusesSchema,
  hasReturns: optionalBooleanSchema,
  page: z.coerce.number().int().min(1).default(1),
''',
              "validators has returns query"
          )
          text = replace_once(
              text,
              '''export type CancelRestockOrderRequest = z.infer<typeof cancelRestockOrderSchema>;
export type ReceiveRestockOrderRequest = z.infer<typeof receiveRestockOrderSchema>;
''',
              '''export type CancelRestockOrderRequest = z.infer<typeof cancelRestockOrderSchema>;
export type SaveRestockReturnReportRequest = z.infer<typeof saveRestockReturnReportSchema>;
export type ReceiveRestockOrderRequest = z.infer<typeof receiveRestockOrderSchema>;
''',
              "validators return type"
          )
          write(path, text)

          # ---------------- backend list filter ----------------
          path = "backend/src/services/restockService.ts"
          text = read(path)
          text = replace_once(
              text,
              '''  const where: Prisma.RestockOrderWhereInput = {
    ...(statuses?.length ? { status: statuses.length === 1 ? statuses[0] : { in: statuses } } : {}),
    ...(query.search ? { orderNumber: { contains: query.search } } : {})
  };
''',
              '''  const where: Prisma.RestockOrderWhereInput = {
    ...(statuses?.length ? { status: statuses.length === 1 ? statuses[0] : { in: statuses } } : {}),
    ...(query.search ? { orderNumber: { contains: query.search } } : {}),
    ...(query.hasReturns
      ? {
          OR: [
            { notes: { contains: "[ReturnReport " } },
            { lines: { some: { notes: { contains: "damage_reason=" } } } }
          ]
        }
      : {})
  };
''',
              "restock return filter"
          )
          write(path, text)

          # ---------------- backend lifecycle metadata ----------------
          path = "backend/src/services/restockLifecycleService.ts"
          text = read(path)
          text = replace_once(
              text,
              '''  AdvanceRestockOrderRequest,
  CancelRestockOrderRequest,
  ReceiveRestockOrderRequest
''',
              '''  AdvanceRestockOrderRequest,
  CancelRestockOrderRequest,
  ReceiveRestockOrderRequest,
  SaveRestockReturnReportRequest
''',
              "lifecycle import type"
          )
          text = replace_once(
              text,
              '''function assertVersion(
''',
              '''type ReturnReportMetadata = {
  createdAt: string;
  deliveryReference: string;
  supplierName: string;
};

const RETURN_REPORT_MARKER_PATTERN = /\\[ReturnReport ([^\\]]+)\\]/;
const RETURN_REPORT_MARKER_PATTERN_GLOBAL = /\\[ReturnReport [^\\]]+\\]/g;

function readReturnReportMetadata(notes: string | null): ReturnReportMetadata | null {
  if (!notes) return null;
  const match = notes.match(RETURN_REPORT_MARKER_PATTERN);
  if (!match?.[1]) return null;

  try {
    const parsed = JSON.parse(decodeURIComponent(match[1])) as Partial<ReturnReportMetadata>;
    if (typeof parsed.createdAt !== "string") return null;
    return {
      createdAt: parsed.createdAt,
      deliveryReference:
        typeof parsed.deliveryReference === "string" ? parsed.deliveryReference : "",
      supplierName: typeof parsed.supplierName === "string" ? parsed.supplierName : ""
    };
  } catch {
    return null;
  }
}

function writeReturnReportMetadata(existing: string | null, metadata: ReturnReportMetadata) {
  const base = existing?.replace(RETURN_REPORT_MARKER_PATTERN_GLOBAL, "").trim() ?? "";
  const marker = `[ReturnReport ${encodeURIComponent(JSON.stringify(metadata))}]`;
  return appendBoundedNote(base || null, marker, 1000);
}

function ensureReturnReportMarker(existing: string | null) {
  if (readReturnReportMetadata(existing)) return existing;
  return writeReturnReportMetadata(existing, {
    createdAt: new Date().toISOString(),
    deliveryReference: "",
    supplierName: ""
  });
}

function hasRecordedReturn(notes: string | null) {
  return Boolean(
    notes && /(?:damaged|other_rejected)=(?!0(?:\\s|$))\\d+/.test(notes)
  );
}

function assertVersion(
''',
              "lifecycle return helpers"
          )
          text = replace_once(
              text,
              '''    const lineById = new Map(order.lines.map((line) => [line.id, line]));

    for (const receiptLine of input.lines) {
''',
              '''    const lineById = new Map(order.lines.map((line) => [line.id, line]));
    let hasReturnUnits = false;

    for (const receiptLine of input.lines) {
''',
              "lifecycle return accumulator"
          )
          text = replace_once(
              text,
              '''      const safeReceiptSummary = receiptSummary({
''',
              '''      if (receiptLine.damagedQuantity > 0 || rejectedQuantity > 0) {
        hasReturnUnits = true;
      }
      const safeReceiptSummary = receiptSummary({
''',
              "lifecycle mark return"
          )
          text = replace_once(
              text,
              '''    await tx.restockOrder.update({
      data: { status: nextStatus },
      where: { id: orderId }
    });
  });

  return getRestockOrder(orderId);
}
''',
              '''    await tx.restockOrder.update({
      data: {
        ...(hasReturnUnits ? { notes: ensureReturnReportMarker(order.notes) } : {}),
        status: nextStatus
      },
      where: { id: orderId }
    });
  });

  return getRestockOrder(orderId);
}

export async function saveRestockReturnReportDocument(
  orderId: string,
  input: SaveRestockReturnReportRequest
) {
  await prisma.$transaction(async (tx) => {
    const existing = await tx.restockOrder.findUnique({
      select: {
        id: true,
        lines: { select: { notes: true } },
        notes: true,
        version: true
      },
      where: { id: orderId }
    });

    if (!existing) {
      throw new HttpError(404, "Restock order was not found.", {
        code: "RESTOCK_ORDER_NOT_FOUND"
      });
    }
    assertVersion(existing, input.expectedVersion);

    const stored = readReturnReportMetadata(existing.notes);
    const returnExists = Boolean(stored) || existing.lines.some((line) => hasRecordedReturn(line.notes));
    if (!returnExists) {
      throw new HttpError(409, "This restock order has no damaged or rejected units to return.", {
        code: "RESTOCK_RETURN_REPORT_NOT_AVAILABLE"
      });
    }

    const notes = writeReturnReportMetadata(existing.notes, {
      createdAt: stored?.createdAt ?? new Date().toISOString(),
      deliveryReference: input.deliveryReference ?? "",
      supplierName: input.supplierName
    });
    const updated = await tx.restockOrder.updateMany({
      data: { notes, version: { increment: 1 } },
      where: { id: orderId, version: input.expectedVersion }
    });

    if (updated.count !== 1) {
      throw new HttpError(409, "The restock order changed elsewhere. Refresh and try again.", {
        code: "RESTOCK_ORDER_VERSION_CONFLICT"
      });
    }
  });

  return getRestockOrder(orderId);
}
''',
              "lifecycle save report"
          )
          write(path, text)

          # ---------------- backend controller ----------------
          path = "backend/src/controllers/restockController.ts"
          text = read(path)
          text = replace_once(
              text,
              '''  receiveRestockOrderSchema,
  replaceRestockOrderLinesSchema,
''',
              '''  receiveRestockOrderSchema,
  replaceRestockOrderLinesSchema,
  saveRestockReturnReportSchema,
''',
              "controller validator import"
          )
          text = replace_once(
              text,
              '''  cancelRestockOrder,
  markRestockOrderAwaitingDelivery,
  receiveRestockOrder
''',
              '''  cancelRestockOrder,
  markRestockOrderAwaitingDelivery,
  receiveRestockOrder,
  saveRestockReturnReportDocument
''',
              "controller service import"
          )
          text += '''\nexport const saveRestockReturnReportController: RequestHandler = async (\n  request,\n  response,\n  next\n) => {\n  try {\n    const params = parseOrThrow(restockOrderIdParamSchema, request.params, {\n      message: "Restock order id is invalid.",\n      code: "INVALID_RESTOCK_ORDER_ID"\n    });\n    const body = parseOrThrow(saveRestockReturnReportSchema, request.body, {\n      message: "Return report details are invalid.",\n      code: "INVALID_RESTOCK_RETURN_REPORT"\n    });\n    const order = await saveRestockReturnReportDocument(params.orderId, body);\n\n    response\n      .status(200)\n      .json(createSuccessResponse("Return report details saved successfully.", order));\n  } catch (error) {\n    next(error);\n  }\n};\n'''
          write(path, text)

          # ---------------- backend route ----------------
          path = "backend/src/routes/restock.routes.ts"
          text = read(path)
          text = replace_once(
              text,
              '''  receiveRestockOrderController,
  replaceRestockOrderLinesController,
''',
              '''  receiveRestockOrderController,
  replaceRestockOrderLinesController,
  saveRestockReturnReportController,
''',
              "route controller import"
          )
          text = replace_once(
              text,
              '''router.post("/:orderId/receipts", receiveRestockOrderController);
''',
              '''router.post("/:orderId/receipts", receiveRestockOrderController);
router.patch("/:orderId/return-report", saveRestockReturnReportController);
''',
              "route return endpoint"
          )
          write(path, text)

          # ---------------- frontend API ----------------
          path = "frontend/src/services/restockApi.ts"
          text = read(path)
          text = replace_once(
              text,
              '''  createdAt: string;
  approvedAt: string | null;
''',
              '''  createdAt: string;
  updatedAt: string;
  approvedAt: string | null;
''',
              "api updated at"
          )
          text = replace_once(
              text,
              '''    statuses?: readonly RestockOrderStatus[];
    page?: number;
''',
              '''    statuses?: readonly RestockOrderStatus[];
    hasReturns?: boolean;
    page?: number;
''',
              "api has returns"
          )
          text += '''\nexport async function saveRestockReturnReport(\n  orderId: string,\n  input: { expectedVersion: number; supplierName: string; deliveryReference?: string | null }\n) {\n  const response = await apiClient.request<RestockOrder, { code?: string; details?: unknown }>(\n    `/api/restock-orders/${encodeURIComponent(orderId)}/return-report`,\n    {\n      method: "PATCH",\n      json: input\n    }\n  );\n\n  if (!response.success || !response.data) {\n    throw new Error(response.message);\n  }\n\n  return response.data;\n}\n'''
          write(path, text)

          # ---------------- return export metadata parser ----------------
          path = "frontend/src/utils/restockReturnExport.ts"
          text = read(path)
          text = replace_once(
              text,
              '''export type RestockReturnDocumentInfo = {
  deliveryReference?: string;
  supplierName?: string;
};
''',
              '''export type RestockReturnDocumentInfo = {
  createdAt?: string;
  deliveryReference?: string;
  supplierName?: string;
};

export type RestockReturnStoredInfo = {
  createdAt: string;
  deliveryReference: string;
  supplierName: string;
};
''',
              "export metadata type"
          )
          text = replace_once(
              text,
              '''const RECEIPT_EVENT_PATTERN =
''',
              '''const RETURN_REPORT_MARKER_PATTERN = /\\[ReturnReport ([^\\]]+)\\]/g;

const RECEIPT_EVENT_PATTERN =
''',
              "export marker regex"
          )
          text = replace_once(
              text,
              '''export function buildRestockReturnSnapshot(
''',
              '''export function getRestockReturnDocumentInfo(order: RestockOrder): RestockReturnStoredInfo {
  const matches = [...(order.notes ?? "").matchAll(RETURN_REPORT_MARKER_PATTERN)];
  const encoded = matches.length > 0 ? matches[matches.length - 1]?.[1] : undefined;

  if (encoded) {
    try {
      const parsed = JSON.parse(decodeURIComponent(encoded)) as Partial<RestockReturnStoredInfo>;
      if (typeof parsed.createdAt === "string") {
        return {
          createdAt: parsed.createdAt,
          deliveryReference:
            typeof parsed.deliveryReference === "string" ? parsed.deliveryReference : "",
          supplierName: typeof parsed.supplierName === "string" ? parsed.supplierName : ""
        };
      }
    } catch {
      // Fall through to the delivery timestamp for legacy damaged tickets.
    }
  }

  return {
    createdAt: order.updatedAt ?? order.approvedAt ?? order.createdAt,
    deliveryReference: "",
    supplierName: ""
  };
}

export function buildRestockReturnSnapshot(
''',
              "export metadata reader"
          )
          text = replace_once(
              text,
              '''    generatedAt: new Date().toISOString(),
''',
              '''    generatedAt: documentInfo.createdAt ?? new Date().toISOString(),
''',
              "export created timestamp"
          )
          write(path, text)

          # ---------------- Return Report dialog persistence ----------------
          path = "frontend/src/components/receiving/ReturnReportDialog.tsx"
          text = read(path)
          text = replace_once(
              text,
              '''import type { RestockOrder } from "@/services/restockApi";
''',
              '''import { saveRestockReturnReport, type RestockOrder } from "@/services/restockApi";
''',
              "return dialog api import"
          )
          text = replace_once(
              text,
              '''  buildRestockReturnSnapshot,
  downloadRestockReturnCsv,
  printRestockReturnCopy
''',
              '''  buildRestockReturnSnapshot,
  downloadRestockReturnCsv,
  getRestockReturnDocumentInfo,
  printRestockReturnCopy
''',
              "return dialog parser import"
          )
          text = replace_once(
              text,
              '''  order
}: {
  onOpenChange: (open: boolean) => void;
  open: boolean;
  order: RestockOrder | null;
}) {
''',
              '''  order,
  onOrderSaved
}: {
  onOpenChange: (open: boolean) => void;
  onOrderSaved?: (order: RestockOrder) => void;
  open: boolean;
  order: RestockOrder | null;
}) {
''',
              "return dialog props"
          )
          text = replace_once(
              text,
              '''  const [deliveryReference, setDeliveryReference] = useState("");
  const snapshot = useMemo(
    () => (order ? buildRestockReturnSnapshot(order, { deliveryReference, supplierName }) : null),
    [deliveryReference, order, supplierName]
  );
''',
              '''  const [deliveryReference, setDeliveryReference] = useState("");
  const storedInfo = useMemo(
    () => (order ? getRestockReturnDocumentInfo(order) : null),
    [order]
  );
  const snapshot = useMemo(
    () =>
      order
        ? buildRestockReturnSnapshot(order, {
            createdAt: storedInfo?.createdAt,
            deliveryReference,
            supplierName
          })
        : null,
    [deliveryReference, order, storedInfo?.createdAt, supplierName]
  );
''',
              "return dialog snapshot"
          )
          text = replace_once(
              text,
              '''  useEffect(() => {
    setSupplierName("");
    setDeliveryReference("");
    setError(null);
  }, [order?.id]);
''',
              '''  useEffect(() => {
    setSupplierName(storedInfo?.supplierName ?? "");
    setDeliveryReference(storedInfo?.deliveryReference ?? "");
    setError(null);
  }, [order?.id, storedInfo?.deliveryReference, storedInfo?.supplierName]);
''',
              "return dialog prefill"
          )
          text = replace_once(
              text,
              '''  async function handlePdf() {
''',
              '''  async function persistDocumentInfo() {
    if (!order || !canExport) return;
    const nextSupplier = supplierName.trim();
    const nextReference = deliveryReference.trim();
    if (
      storedInfo?.supplierName === nextSupplier &&
      storedInfo.deliveryReference === nextReference
    ) {
      return;
    }

    const saved = await saveRestockReturnReport(order.id, {
      deliveryReference: nextReference || null,
      expectedVersion: order.version,
      supplierName: nextSupplier
    });
    onOrderSaved?.(saved);
  }

  async function handlePdf() {
''',
              "return dialog persist helper"
          )
          text = replace_once(
              text,
              '''    try {
      const { downloadRestockReturnPdf } = await import("@/utils/restockReturnPdf");
''',
              '''    try {
      await persistDocumentInfo();
      const { downloadRestockReturnPdf } = await import("@/utils/restockReturnPdf");
''',
              "return dialog pdf persist"
          )
          text = replace_once(
              text,
              '''  function handlePrint() {
    if (!snapshot) return;
    setBusy("print");
    setError(null);
    try {
      if (!printRestockReturnCopy(snapshot)) {
        setError("Pop-up was blocked. Allow pop-ups for Ysabelle Store and try again.");
        return;
      }
      onOpenChange(false);
    } finally {
      setBusy(null);
    }
  }

  function handleCsv() {
    if (!snapshot) return;
    setBusy("csv");
    setError(null);
    try {
      downloadRestockReturnCsv(snapshot);
      onOpenChange(false);
    } finally {
      setBusy(null);
    }
  }
''',
              '''  async function handlePrint() {
    if (!snapshot) return;
    setBusy("print");
    setError(null);
    try {
      await persistDocumentInfo();
      if (!printRestockReturnCopy(snapshot)) {
        setError("Pop-up was blocked. Allow pop-ups for Ysabelle Store and try again.");
        return;
      }
      onOpenChange(false);
    } catch (requestError) {
      setError(
        requestError instanceof Error ? requestError.message : "The return report could not be saved."
      );
    } finally {
      setBusy(null);
    }
  }

  async function handleCsv() {
    if (!snapshot) return;
    setBusy("csv");
    setError(null);
    try {
      await persistDocumentInfo();
      downloadRestockReturnCsv(snapshot);
      onOpenChange(false);
    } catch (requestError) {
      setError(
        requestError instanceof Error ? requestError.message : "The return report could not be saved."
      );
    } finally {
      setBusy(null);
    }
  }
''',
              "return dialog print csv async"
          )
          text = text.replace('onClick={handlePrint}', 'onClick={() => void handlePrint()}')
          text = text.replace('onClick={handleCsv}', 'onClick={() => void handleCsv()}')
          write(path, text)

          # ---------------- new Return History dialog ----------------
          write(
              "frontend/src/components/receiving/ReturnHistoryDialog.tsx",
              '''import { LoaderCircle, RotateCcw } from "lucide-react";
import { useEffect, useMemo, useState } from "react";

import { AppPagination } from "@/components/shared/AppPagination";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle
} from "@/components/ui/dialog";
import {
  listRestockOrders,
  type PaginationMeta,
  type RestockOrder
} from "@/services/restockApi";
import {
  buildRestockReturnSnapshot,
  getRestockReturnDocumentInfo
} from "@/utils/restockReturnExport";

const EMPTY_META: PaginationMeta = {
  page: 1,
  pageSize: 10,
  totalItems: 0,
  totalPages: 0
};

const dateFormatter = new Intl.DateTimeFormat("en-PH", {
  dateStyle: "medium",
  timeZone: "Asia/Manila"
});

export function ReturnHistoryDialog({
  onOpenChange,
  onView,
  open
}: {
  onOpenChange: (open: boolean) => void;
  onView: (order: RestockOrder) => void;
  open: boolean;
}) {
  const [items, setItems] = useState<RestockOrder[]>([]);
  const [meta, setMeta] = useState<PaginationMeta>(EMPTY_META);
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(10);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    if (!open) return;
    const controller = new AbortController();
    setLoading(true);
    setError(null);

    void listRestockOrders(
      { hasReturns: true, page, pageSize },
      { signal: controller.signal }
    )
      .then((result) => {
        setItems(result.items);
        setMeta(result.meta);
        const lastPage = Math.max(1, result.meta.totalPages);
        if (page > lastPage) setPage(lastPage);
      })
      .catch((requestError) => {
        if (controller.signal.aborted) return;
        setError(
          requestError instanceof Error
            ? requestError.message
            : "Return history could not be loaded."
        );
      })
      .finally(() => {
        if (!controller.signal.aborted) setLoading(false);
      });

    return () => controller.abort();
  }, [open, page, pageSize]);

  const rows = useMemo(
    () =>
      items.map((order) => {
        const info = getRestockReturnDocumentInfo(order);
        const snapshot = buildRestockReturnSnapshot(order, info);
        return { info, order, snapshot };
      }),
    [items]
  );

  return (
    <Dialog onOpenChange={onOpenChange} open={open}>
      <DialogContent className="max-h-[82vh] max-w-[920px] grid-rows-[auto_minmax(0,1fr)] gap-0">
        <DialogHeader className="border-b border-slate-200 px-6 py-5">
          <div className="flex items-center gap-2">
            <RotateCcw aria-hidden="true" className="h-5 w-5 text-amber-600" />
            <DialogTitle>Return history</DialogTitle>
          </div>
          <DialogDescription>
            Reopen supplier return documents for damaged or rejected deliveries.
          </DialogDescription>
        </DialogHeader>

        <div className="min-h-0 overflow-y-auto p-6">
          {error ? (
            <Alert variant="destructive">
              <AlertTitle>Return history needs attention</AlertTitle>
              <AlertDescription>{error}</AlertDescription>
            </Alert>
          ) : null}

          {loading && items.length === 0 ? (
            <div className="flex items-center justify-center gap-2 py-14 text-sm text-slate-500">
              <LoaderCircle aria-hidden="true" className="h-4 w-4 animate-spin" />
              Loading return history…
            </div>
          ) : null}

          {!loading && !error && rows.length === 0 ? (
            <div className="rounded-lg border border-dashed border-slate-200 px-5 py-12 text-center">
              <p className="text-sm font-semibold text-slate-900">No return reports yet.</p>
              <p className="mt-1 text-xs text-slate-500">
                Damaged or rejected receiving records will appear here automatically.
              </p>
            </div>
          ) : null}

          {rows.length > 0 ? (
            <div className="overflow-hidden rounded-xl border border-slate-200 bg-white">
              {rows.map(({ info, order, snapshot }) => {
                const units =
                  snapshot?.lines.reduce((sum, line) => sum + line.returnQuantity, 0) ?? 0;
                return (
                  <div
                    className="flex flex-col gap-3 border-b border-slate-100 px-4 py-4 last:border-b-0 sm:flex-row sm:items-center sm:justify-between"
                    key={order.id}
                  >
                    <div className="min-w-0">
                      <div className="flex flex-wrap items-center gap-2">
                        <p className="truncate text-sm font-semibold text-slate-950">
                          {snapshot?.returnReference ?? `RETURN-${order.orderNumber}`}
                        </p>
                        <Badge variant="warning">
                          {units.toLocaleString()} return unit{units === 1 ? "" : "s"}
                        </Badge>
                      </div>
                      <p className="mt-1 text-xs text-slate-500">
                        {order.orderNumber} · {dateFormatter.format(new Date(info.createdAt))}
                      </p>
                      <p className="mt-1 text-xs text-slate-600">
                        {info.supplierName || "Supplier / manufacturer not recorded yet"}
                      </p>
                    </div>
                    <Button
                      onClick={() => onView(order)}
                      size="sm"
                      type="button"
                      variant="secondary"
                    >
                      View report
                    </Button>
                  </div>
                );
              })}
            </div>
          ) : null}

          {meta.totalItems > 0 ? (
            <AppPagination
              className="mt-4"
              isLoading={loading}
              itemLabel="return reports"
              onPageChange={setPage}
              onPageSizeChange={(nextSize) => {
                setPageSize(nextSize);
                setPage(1);
              }}
              page={page}
              pageSize={pageSize}
              totalItems={meta.totalItems}
              totalPages={meta.totalPages}
            />
          ) : null}
        </div>
      </DialogContent>
    </Dialog>
  );
}
'''
          )

          # ---------------- Receiving page pagination + history action ----------------
          path = "frontend/src/pages/ReceivingPage.tsx"
          text = read(path)
          text = replace_once(
              text,
              '''import { ReturnReportDialog } from "@/components/receiving/ReturnReportDialog";
import { PageHeader } from "@/components/shared/PageHeader";
''',
              '''import { ReturnHistoryDialog } from "@/components/receiving/ReturnHistoryDialog";
import { ReturnReportDialog } from "@/components/receiving/ReturnReportDialog";
import { AppPagination } from "@/components/shared/AppPagination";
import { PageHeader } from "@/components/shared/PageHeader";
''',
              "receiving imports"
          )
          text = replace_once(
              text,
              '''type QueueState = {
  history: RestockOrder[];
  historyTotal: number;
  partial: RestockOrder[];
  partialTotal: number;
  ready: RestockOrder[];
  readyTotal: number;
};

const EMPTY_QUEUE: QueueState = {
  history: [],
  historyTotal: 0,
  partial: [],
  partialTotal: 0,
  ready: [],
  readyTotal: 0
};
''',
              '''type QueueState = {
  history: RestockOrder[];
  historyTotal: number;
  historyTotalPages: number;
  partial: RestockOrder[];
  partialTotal: number;
  partialTotalPages: number;
  ready: RestockOrder[];
  readyTotal: number;
  readyTotalPages: number;
  returnTotal: number;
};

type QueuePages = Record<ReceivingView, number>;

const EMPTY_QUEUE: QueueState = {
  history: [],
  historyTotal: 0,
  historyTotalPages: 0,
  partial: [],
  partialTotal: 0,
  partialTotalPages: 0,
  ready: [],
  readyTotal: 0,
  readyTotalPages: 0,
  returnTotal: 0
};

const INITIAL_QUEUE_PAGES: QueuePages = { history: 1, partial: 1, ready: 1 };
''',
              "receiving queue shape"
          )
          text = replace_once(
              text,
              '''  const [queue, setQueue] = useState<QueueState>(EMPTY_QUEUE);
  const [view, setView] = useState<ReceivingView>("ready");
  const [loading, setLoading] = useState(true);
''',
              '''  const [queue, setQueue] = useState<QueueState>(EMPTY_QUEUE);
  const [view, setView] = useState<ReceivingView>("ready");
  const [queuePages, setQueuePages] = useState<QueuePages>(INITIAL_QUEUE_PAGES);
  const [queuePageSize, setQueuePageSize] = useState(20);
  const [loading, setLoading] = useState(true);
''',
              "receiving pagination state"
          )
          text = replace_once(
              text,
              '''  const [returnReportOrder, setReturnReportOrder] = useState<RestockOrder | null>(null);

  const loadQueue = useCallback(async () => {
''',
              '''  const [returnReportOrder, setReturnReportOrder] = useState<RestockOrder | null>(null);
  const [returnHistoryOpen, setReturnHistoryOpen] = useState(false);

  const loadQueue = useCallback(async () => {
''',
              "receiving history state"
          )
          old_load = '''  const loadQueue = useCallback(async () => {
    setLoading(true);
    setError(null);

    try {
      const [readyResult, partialResult, historyResult] = await Promise.all([
        listRestockOrders({ page: 1, pageSize: 100, statuses: READY_STATUSES }),
        listRestockOrders({ page: 1, pageSize: 100, status: "PARTIALLY_RECEIVED" }),
        listRestockOrders({ page: 1, pageSize: 100, status: "RECEIVED" })
      ]);

      setQueue({
        history: historyResult.items,
        historyTotal: historyResult.meta.totalItems,
        partial: partialResult.items,
        partialTotal: partialResult.meta.totalItems,
        ready: readyResult.items,
        readyTotal: readyResult.meta.totalItems
      });
    } catch (requestError) {
      setError(
        requestError instanceof Error
          ? requestError.message
          : "Restock delivery tickets could not be loaded."
      );
    } finally {
      setLoading(false);
    }
  }, []);
'''
          new_load = '''  const loadQueue = useCallback(async () => {
    setLoading(true);
    setError(null);

    try {
      const [readyResult, partialResult, historyResult, returnResult] = await Promise.all([
        listRestockOrders({
          page: queuePages.ready,
          pageSize: queuePageSize,
          statuses: READY_STATUSES
        }),
        listRestockOrders({
          page: queuePages.partial,
          pageSize: queuePageSize,
          status: "PARTIALLY_RECEIVED"
        }),
        listRestockOrders({
          page: queuePages.history,
          pageSize: queuePageSize,
          status: "RECEIVED"
        }),
        listRestockOrders({ hasReturns: true, page: 1, pageSize: 1 })
      ]);

      setQueue({
        history: historyResult.items,
        historyTotal: historyResult.meta.totalItems,
        historyTotalPages: historyResult.meta.totalPages,
        partial: partialResult.items,
        partialTotal: partialResult.meta.totalItems,
        partialTotalPages: partialResult.meta.totalPages,
        ready: readyResult.items,
        readyTotal: readyResult.meta.totalItems,
        readyTotalPages: readyResult.meta.totalPages,
        returnTotal: returnResult.meta.totalItems
      });

      setQueuePages((current) => {
        const next = {
          history: Math.min(current.history, Math.max(1, historyResult.meta.totalPages)),
          partial: Math.min(current.partial, Math.max(1, partialResult.meta.totalPages)),
          ready: Math.min(current.ready, Math.max(1, readyResult.meta.totalPages))
        };
        return next.history === current.history &&
          next.partial === current.partial &&
          next.ready === current.ready
          ? current
          : next;
      });
    } catch (requestError) {
      setError(
        requestError instanceof Error
          ? requestError.message
          : "Restock delivery tickets could not be loaded."
      );
    } finally {
      setLoading(false);
    }
  }, [queuePageSize, queuePages.history, queuePages.partial, queuePages.ready]);
'''
          text = replace_once(text, old_load, new_load, "receiving loader")
          text = replace_once(
              text,
              '''  const visibleTotal =
    view === "ready"
      ? queue.readyTotal
      : view === "partial"
        ? queue.partialTotal
        : queue.historyTotal;
''',
              '''  const visibleTotal =
    view === "ready"
      ? queue.readyTotal
      : view === "partial"
        ? queue.partialTotal
        : queue.historyTotal;
  const visibleTotalPages =
    view === "ready"
      ? queue.readyTotalPages
      : view === "partial"
        ? queue.partialTotalPages
        : queue.historyTotalPages;
  const visiblePage = queuePages[view];
''',
              "receiving visible pagination"
          )
          text = replace_once(
              text,
              '''  function changeView(next: ReceivingView) {
    setView(next);
    setExpandedTicketId(null);
  }
''',
              '''  function changeView(next: ReceivingView) {
    setView(next);
    setExpandedTicketId(null);
  }

  function changeQueuePage(page: number) {
    setQueuePages((current) => ({ ...current, [view]: page }));
    setExpandedTicketId(null);
  }

  function changeQueuePageSize(pageSize: number) {
    setQueuePageSize(pageSize);
    setQueuePages(INITIAL_QUEUE_PAGES);
    setExpandedTicketId(null);
  }
''',
              "receiving page handlers"
          )
          text = replace_once(
              text,
              '''      <PageHeader
        eyebrow="Inventory"
        title="Receiving"
        description="Approved restock tickets move here automatically. Review a ticket and record only the delivery exceptions."
      />
''',
              '''      <PageHeader
        actions={
          <Button onClick={() => setReturnHistoryOpen(true)} type="button" variant="secondary">
            <RotateCcw aria-hidden="true" className="h-4 w-4" />
            Return history
            {queue.returnTotal > 0 ? <Badge variant="warning">{queue.returnTotal}</Badge> : null}
          </Button>
        }
        eyebrow="Inventory"
        title="Receiving"
        description="Approved restock tickets move here automatically. Review a ticket and record only the delivery exceptions."
      />
''',
              "receiving header action"
          )
          text = replace_once(
              text,
              '''        {visibleTotal > visibleOrders.length ? (
          <p className="border-t border-slate-100 px-5 py-3 text-xs text-slate-500">
            Showing the latest {visibleOrders.length.toLocaleString()} of{" "}
            {visibleTotal.toLocaleString()} tickets.
          </p>
        ) : null}
''',
              '''        {visibleTotal > 0 ? (
          <AppPagination
            className="m-4"
            isLoading={loading}
            itemLabel="tickets"
            onPageChange={changeQueuePage}
            onPageSizeChange={changeQueuePageSize}
            page={visiblePage}
            pageSize={queuePageSize}
            totalItems={visibleTotal}
            totalPages={visibleTotalPages}
          />
        ) : null}
''',
              "receiving pagination component"
          )
          text = replace_once(
              text,
              '''      <ReturnReportDialog
        onOpenChange={(open) => {
          if (!open) setReturnReportOrder(null);
        }}
        open={Boolean(returnReportOrder)}
        order={returnReportOrder}
      />
''',
              '''      <ReturnHistoryDialog
        onOpenChange={setReturnHistoryOpen}
        onView={(order) => {
          setReturnHistoryOpen(false);
          setReturnReportOrder(order);
        }}
        open={returnHistoryOpen}
      />

      <ReturnReportDialog
        onOpenChange={(open) => {
          if (!open) setReturnReportOrder(null);
        }}
        onOrderSaved={(order) => {
          setReturnReportOrder(order);
          void loadQueue();
        }}
        open={Boolean(returnReportOrder)}
        order={returnReportOrder}
      />
''',
              "receiving history dialog"
          )
          write(path, text)

          # ---------------- UI contract ----------------
          path = "scripts/restock-phase7-9-ui-contract-test.ts"
          text = read(path)
          text = replace_once(
              text,
              '''const returnDialogSource = readFileSync(
  resolve(process.cwd(), "src/components/receiving/ReturnReportDialog.tsx"),
  "utf8"
);
''',
              '''const returnDialogSource = readFileSync(
  resolve(process.cwd(), "src/components/receiving/ReturnReportDialog.tsx"),
  "utf8"
);
const returnHistorySource = readFileSync(
  resolve(process.cwd(), "src/components/receiving/ReturnHistoryDialog.tsx"),
  "utf8"
);
''',
              "ui contract history source"
          )
          text = replace_once(
              text,
              '''assert.match(receivingSource, /ReturnReportDialog/);
''',
              '''assert.match(receivingSource, /ReturnReportDialog/);
assert.match(receivingSource, /ReturnHistoryDialog/);
assert.match(receivingSource, /<AppPagination/);
assert.match(receivingSource, /Return history/);
''',
              "ui contract receiving qol"
          )
          text = replace_once(
              text,
              '''assert.match(returnDialogSource, /deliveryReference/);
''',
              '''assert.match(returnDialogSource, /deliveryReference/);
assert.match(returnDialogSource, /saveRestockReturnReport/);
assert.match(returnDialogSource, /getRestockReturnDocumentInfo/);
assert.match(returnHistorySource, /<AppPagination/);
assert.match(returnHistorySource, /hasReturns: true/);
assert.match(returnHistorySource, /View report/);
''',
              "ui contract return persistence"
          )
          text = replace_once(
              text,
              '''assert.match(apiSource, /damageReason/);
''',
              '''assert.match(apiSource, /damageReason/);
assert.match(apiSource, /hasReturns/);
assert.match(apiSource, /return-report/);
''',
              "ui contract api return"
          )
          write(path, text)

          # ---------------- backend contract ----------------
          path = "backend/test/restock-phase7-9-contract.test.ts"
          text = read(path)
          text = replace_once(
              text,
              '''  cancelRestockOrderSchema,
  receiveRestockOrderSchema
''',
              '''  cancelRestockOrderSchema,
  receiveRestockOrderSchema,
  saveRestockReturnReportSchema
''',
              "backend test validator import"
          )
          text = replace_once(
              text,
              '''const receivingSource = readFileSync(
''',
              '''const restockServiceSource = readFileSync(
  new URL("../src/services/restockService.ts", import.meta.url),
  "utf8"
);
const receivingSource = readFileSync(
''',
              "backend test service source"
          )
          text = replace_once(
              text,
              '''  assert.match(restockRouteSource, /\\/:orderId\\/receipts/);
''',
              '''  assert.match(restockRouteSource, /\\/:orderId\\/receipts/);
  assert.match(restockRouteSource, /\\/:orderId\\/return-report/);
''',
              "backend test route"
          )
          text += '''\n\ntest("Return report details are validated and return history has a server-side filter", () => {\n  assert.equal(\n    saveRestockReturnReportSchema.safeParse({\n      expectedVersion: 3,\n      supplierName: "Example Supplier",\n      deliveryReference: "DR-001"\n    }).success,\n    true\n  );\n  assert.equal(\n    saveRestockReturnReportSchema.safeParse({\n      expectedVersion: 3,\n      supplierName: ""\n    }).success,\n    false\n  );\n  assert.match(lifecycleSource, /ReturnReport/);\n  assert.match(lifecycleSource, /saveRestockReturnReportDocument/);\n  assert.match(restockServiceSource, /query\\.hasReturns/);\n  assert.match(restockServiceSource, /damage_reason=/);\n});\n'''
          write(path, text)
          PY

          if git diff --quiet; then
            echo "No changes required."
            exit 0
          fi

          git config user.name "Armando R. Abarado Jr."
          git config user.email "abaradoaj@gmail.com"
          git add backend frontend scripts
          git commit -m "feat(receiving): add return history and pagination"
          HUSKY=0 git push origin HEAD:sprint/v0.10/sprint-10
