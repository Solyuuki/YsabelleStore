name: Phase 6 Format

on:
  push:
    branches:
      - "sprint/v0.10/sprint-10"
    paths:
      - ".github/workflows/phase6-format.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  format-phase6:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: sprint/v0.10/sprint-10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply supplier restock export QoL pass
        run: python scripts/apply-restock-supplier-qol.py
      - name: Format supplier export changes
        run: npx prettier --write frontend/src/components/reports/RestockPlanningPanel.tsx frontend/src/pages/ReportsPage.tsx frontend/src/utils/restockExport.ts scripts/restock-phase4-6-ui-contract-test.ts
      - name: Verify formatting
        run: npx prettier --check frontend/src/components/reports/RestockPlanningPanel.tsx frontend/src/pages/ReportsPage.tsx frontend/src/utils/restockExport.ts scripts/restock-phase4-6-ui-contract-test.ts
      - name: Run frontend contracts
        run: npm run test --workspace frontend
      - name: Build frontend
        run: npm run build --workspace frontend
      - name: Commit supplier export pass if needed
        shell: bash
        run: |
          if git diff --quiet -- frontend/src/components/reports/RestockPlanningPanel.tsx frontend/src/pages/ReportsPage.tsx frontend/src/utils/restockExport.ts scripts/restock-phase4-6-ui-contract-test.ts; then
            echo "Supplier restock export changes are already applied."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend/src/components/reports/RestockPlanningPanel.tsx frontend/src/pages/ReportsPage.tsx frontend/src/utils/restockExport.ts scripts/restock-phase4-6-ui-contract-test.ts
          HUSKY=0 git commit -m "feat(restock): add supplier-ready order export"
          HUSKY=0 git push origin HEAD:sprint/v0.10/sprint-10
